{

Program: Calculate threat level
Dept. of Unified Protection - Threat Systems Division

Inputs via stack:
( SuspectID  something something something )

Outputs via stack:
( TAL )
The TAL (Threat Assessment Level) is an integer from 0 to 100, with 0 being of
minimal interest and 100 being an emergent threat.

Modification of this algorithm is unauthorized without prior review by the DUP
Threat Authority Board.

}

{ Store values }
c:b:a:i:

i;