Copper
======

Setup
-----
1. Install Node (nodejs.org) and npm (http://npmjs.org).

2. Install the Node modules required to build via:

  ```bash
  npm install
  ```

Build
-----
From this directory:

```bash
grunt
```